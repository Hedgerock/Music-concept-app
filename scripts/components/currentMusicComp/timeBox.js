import { createElem } from "../../functions/createFunc/createEl.js";

export function timeBoxFunc() {

    const timeBox = createElem({ 
        tag: 'div', 
        className: 'time-box' 
    });


    return timeBox
}